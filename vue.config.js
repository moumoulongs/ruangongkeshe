// 假设要请求的接口是：http://40.00.100.100:3002/api/user/add
module.exports = {
    devServer:{
      host:'127.0.0.1',  // 本地主机
        port:8081,  // 端口号的配置
        open:true,  // 自动打开浏览器
    }
  }

  // 注意：修改了配置文件后一定要重启才会生效;
